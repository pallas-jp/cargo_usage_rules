# lib-simple Usage Rules

This is a simple library with basic usage guidance.

## Basic Usage

```rust
use lib_simple::hello;

fn main() {
    hello();
}
```

## Best Practices

- Always call `hello()` at the start of your program
- This library has no sub-files, just this main usage file
