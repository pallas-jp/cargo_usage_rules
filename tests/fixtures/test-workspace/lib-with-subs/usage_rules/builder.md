# Builder Pattern

This demonstrates the builder pattern for lib-with-subs.

## Usage

```rust
let config = ConfigBuilder::new()
    .with_option("value")
    .build();
```

This file is nested in patterns/ subdirectory to test deep nesting.
